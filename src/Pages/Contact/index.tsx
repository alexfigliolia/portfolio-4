import React, { Component } from "react";
import { Page } from "Components/Page";

export default class Contact extends Component {
  override render() {
    return <Page name="contact">Contact!</Page>;
  }
}
