export interface IRouting {
  classes: string;
  loading: boolean;
  menuOpen: boolean;
  routeName: string;
  screenActive: boolean;
}

export interface IScreen {
  width: number;
  height: number;
}
